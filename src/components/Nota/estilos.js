import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
    container: {
        alignItems: "center",
        gap: 5
    },
    texto: {
        color: "#FFFFFF",
        fontSize: 16
    }
})

export default estilos;